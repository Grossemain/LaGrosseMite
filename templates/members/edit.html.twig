{% extends 'admin.html.twig' %}

{% block title %}Editer un Membre
{% endblock %}

{% block body %}
	<h1 class="text-center">Editer un Membre</h1>

	{{ include('members/_form.html.twig', {'button_label': 'Update'}) }}
	<div class="text-center">
		<a href="{{ path('app_members_index') }}">Retour à la liste</a>
	</div>
	<div class="text-center">
		{{ include('members/_delete_form.html.twig') }}
	</div>
{% endblock %}
