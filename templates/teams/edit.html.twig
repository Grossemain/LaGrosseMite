{% extends 'admin.html.twig' %}

{% block title %}Editer une Team
{% endblock %}

{% block body %}
	<h1 class="text-center">Editer la Team</h1>

	{{ include('teams/_form.html.twig', {'button_label': 'Update'}) }}

	<div class="text-center">
		<a href="{{ path('app_teams_index') }}">Retour à la liste</a>
	</div>
	<div class="text-center">
	{{ include('teams/_delete_form.html.twig') }}
</div>
{% endblock %}
