{% extends 'base.html.twig' %}

{% block title %}Editer une technique
{% endblock %}

{% block body %}
	<h1 class="text-center">Editer une technique</h1>

	{{ include('sections/_form.html.twig', {'button_label': 'Update'}) }}
	<div class="text-center">
		<a href="{{ path('app_sections_index') }}">Retour à la liste</a>
	
	<div class="text-center">
	{{ include('sections/_delete_form.html.twig') }}
</div>
{% endblock %}
